# Archive

This folder contains archived implementations that are not the active production path.

- `web-customer-legacy/`: Legacy React + Vite customer app.

Current active customer app:

- `../customer_web/` (Flutter Web)
